// join method to join each element of an array into a string separated by whatever delimiter you provide as an argument

var toJoin = [ 'Split', 'me', 'into', 'an', 'array' ];
var joined = '';

joined = toJoin.join(" ");
console.log(joined);